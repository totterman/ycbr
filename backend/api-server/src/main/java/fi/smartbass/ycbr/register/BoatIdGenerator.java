package fi.smartbass.ycbr.register;
/*
import org.springframework.data.relational.core.mapping.eventId.BeforeConvertCallback;
import org.springframework.stereotype.Component;

@Component
public class BoatIdGenerator implements BeforeConvertCallback<BoatEntity> {
    @Override
    public BoatEntity onBeforeConvert(BoatEntity aggregate) {
        if (aggregate.getBoatId() == null) {
            aggregate.setBoatId(java.util.UUID.randomUUID());
        }
        return aggregate;
    }
}
*/